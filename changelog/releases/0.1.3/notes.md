This release renames the npm package to pi-terminal-theme and updates install instructions to use the unscoped package name.

## 🔧 Changes

### Rename npm package to pi-terminal-theme

Renamed the npm package from @mavam/pi-terminal-theme to pi-terminal-theme and updated install instructions to use the unscoped package name.

*By @mavam.*
